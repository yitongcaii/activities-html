---
name: culture-event-sop
description: >
  文化类活动SOP助手。帮助BP/ABP/活动组织者筹备员工大会、leader offsite、团建、Openday、
  工作坊、GM面对面等文化活动。提供：场景分析→活动方案推荐→完整筹备checklist&时间线→
  参考模板→注意事项→案例参考→案例沉淀模板生成。支持查询特定环节怎么做、查找工具资源。
  支持生成Word/Excel/HTML格式的统一筹备文件。
  触发词：文化活动、员工大会、leader offsite、团建筹备、文化SOP、活动checklist、
  活动方案、活动筹备、文化运营、BP活动、年会筹备、活动排期、案例沉淀、活动模板、
  生成文档、导出方案、下载筹备表、表彰大会、颁奖活动、庆功会、活动流程、活动分工、
  rundown、场地安排、高管谈参、QA问卷、会议扩容、直播方案、技术联调、活动预算、
  活动礼品、团建游戏、破冰活动、暖场环节、文化传递、文化价值观、GM面对面、工作坊、
  下午茶、Openday、开放日、活动总结、活动复盘、活动反馈、员工调研、文化访谈、
  活动主持、主持稿、活动摄影、活动录像、数智人主持、AI互动、抽奖环节、颁奖词、
  奖杯、花束、活动音乐、BGM、活动海报、活动视觉、活动物料、活动彩排、活动执行、
  活动运营、活动归档
maintainer: yitong
---

# 文化活动SOP助手

prompts:
  # 场景A：活动全流程规划
  - name: plan_activity
    description: 用户输入活动类型（如"员工大会"），输出完整筹备方案
    template: prompts/plan_activity.md

  # 场景B：场景匹配推荐
  - name: recommend_by_context
    description: 用户输入部门当前状态（如"组织架构调整期"），推荐适合的文化活动
    template: prompts/recommend_by_context.md

  # 场景C：环节细节查询
  - name: query_detail
    description: 用户查询某个具体环节怎么做（如"QA问卷怎么设计"）
    template: prompts/query_detail.md

  # 场景D：案例沉淀模板
  - name: case_template
    description: 活动完成后生成案例沉淀模板
    template: prompts/case_template.md

  # 场景E：排期表模板
  - name: schedule_template
    description: 生成部门文化活动排期表模板
    template: prompts/schedule_template.md

  # 场景F：工具资源查询
  - name: tool_query
    description: 查询游戏/BGM/素材/模板等工具资源
    template: prompts/tool_query.md

workflow:
  1. **信息收集**（关键！）
     - 先检查用户是否已提供足够信息
     - 如果信息不足，调用 prompts/info_collection.md 进行引导提问
     - 每次最多问3个问题，提供选项让用户选择
     - 必须收集的信息（按优先级）：
       - P0: 活动类型、活动目标/背景
       - P1: 活动日期、线上/线下、预计人数、预算
       - P2: 高管参与情况（GM/Dowson是否参加）、特殊要求
  2. 识别用户意图，匹配到上述6个场景之一
  3. 读取 references/extracted_data.json 中的对应数据
  4. 按对应 prompt 模板组织输出（传入收集到的所有变量）
  5. 输出格式：默认Markdown表格+结构化文本；用户明确要求时调用 scripts/gen_doc.py 生成 Word/Excel/HTML

interaction_model:
  mode: 对话式引导
  description: |
    不要一次性输出完整方案。采用"收集信息 → 确认理解 → 输出方案"的对话流程：
    
    Round 1: 用户提问 → AI判断信息是否足够
      - 不够：调用 info_collection.md 反问1-3个问题
      - 足够：进入Round 2
    
    Round 2: 用户补充信息 → AI继续判断
      - 还有缺失：继续追问（每次最多3个）
      - 信息完整：输出初步方案框架，让用户确认
    
    Round 3: 用户确认或调整 → AI输出完整方案
      - 可以问"这个方向对吗？需要调整什么？"
      - 确认后输出完整方案
      - 最后问"需要我生成Word/Excel/HTML文件吗？"

data_source:
  path: references/extracted_data.json
  format: JSON
  sheets:
    - name: "【第一步】厘清Why"
      content: 7个业务触发场景 + 活动目标分层（员工/GM/部门）+ 活动类型分层
    - name: "【第二步】持续完善的指引"
      content: 全流程SOP（会前/会中/会后），含环节、内容、责任人、完成标准
    - name: "访谈提纲&模板规划"
      content: 文化对接人访谈提纲 + 活动案例模板设计
    - name: "【花草团队】各部门文化类活动收集"
      content: 各部门真实文化活动案例（医疗/多媒体/量子/安全等），案例展示时隐藏对接人信息
    - name: "会议暖场及表彰SOP补充"
      content: 互动热场、表彰颁奖、补充装备包（游戏/BGM/素材）
    - name: "2025部门Leader offsite&员工大会安排"
      content: 各部门历史活动排期记录，仅在用户主动询问时提供参考

doc_generator:
  script: scripts/gen_doc.py
  supported_formats: ["word", "excel", "html", "all"]
  usage: |
    当用户明确要求生成文件时，调用 gen_doc.py 生成统一筹备文档：
    python scripts/gen_doc.py --format [word|excel|html|all] --input <json> --output <路径>
    
    生成的统一文件包含以下模块：
    - 活动定位与目标
    - 通用筹备时间线（会前/会中/会后）
    - 通用筹备清单
    - 专项补充清单（根据活动类型差异化）
    - 同类活动案例参考
    - 风险提示与注意事项
    - 参考模板与资源

---

## 使用场景（覆盖范围）

### 直接场景（用户明确说出活动类型或需求）
1. **活动全流程规划** — "我要筹备员工大会" → 输出完整方案（目标+时间线+checklist+案例+风险）
2. **场景匹配推荐** — "我们部门刚做了组织架构调整" → 分析场景→推荐活动类型→给出理由
3. **环节细节查询** — "QA问卷怎么设计" / "高管谈参怎么写" → 操作步骤+完成标准+坑点+资源
4. **案例沉淀** — "帮我生成案例沉淀模板" → 标准8模块模板
5. **排期规划** — "帮我做年度文化活动排期" → 季度排期表+里程碑
6. **工具资源查询** — "有什么团建小游戏" / "颁奖BGM推荐" → 分类资源列表

### 间接场景（用户问题较模糊，需要引导）
7. **"文化活动怎么做"** → 反问：部门当前处于什么阶段？面向什么人群？
8. **"第一次做BP，不知道怎么搞活动"** → 输出通用入门指南+推荐阅读顺序
9. **"活动做完了，怎么写总结"** → 引导使用案例沉淀模板
10. **"有没有什么参考"** → 展示同类部门案例（隐藏对接人）

### 文档输出场景
11. **"生成Word/Excel/HTML"** → 调用 gen_doc.py 输出统一文件
12. **"帮我导出筹备表"** → 生成Excel格式的checklist
13. **"给我一份完整的方案文档"** → 生成Word文档

---

## 长期使用需补充的材料

### 高优先级（直接影响skill效果）
| 材料类型 | 具体内容 | 补充方式 |
|---------|---------|---------|
| **案例沉淀资料链接** | 医疗、多媒体、量子、安全等案例的KM文章链接/图片/视频 | 向各文化对接人收集 |
| **更多部门案例** | 企点、医疗&生命科学、云技服、云产品等部门的详细案例 | 访谈文化对接人后录入 |
| **活动类型扩展** | 工作坊/下午茶围谈/Openday/表彰大会/班委会 的SOP | 从现有部门实践中提炼 |
| **模板文件** | 主持稿模板、颁奖PPT模板、流程音乐包等实际文件 | 整理现有资源并上传 |

### 中优先级（提升skill深度）
| 材料类型 | 具体内容 |
|---------|---------|
| **访谈记录** | 文化对接人访谈的完整Q&A |
| **预算参考** | 各类活动的预算区间参考 |
| **供应商资源** | 场地/餐饮/礼品/摄影等供应商名单 |
| **高管谈参范例** | Dowson/Wang/GM的过往谈参脱敏版 |

### 低优先级（锦上添花）
| 材料类型 | 具体内容 |
|---------|---------|
| **活动照片库** | 各类活动的现场照片（脱敏后） |
| **视频素材库** | 暖场视频、颁奖视频范例 |
| **数据看板** | 活动效果的数据化跟踪模板 |

---

## 注意事项

1. **对接人信息**：案例展示时隐藏文化对接人姓名，只展示部门和活动信息
2. **历史排期**：Sheet6的各部门历史排期仅在用户主动询问时提供，不作为默认输出
3. **输出格式**：
   - 默认：Markdown表格 + 结构化文本（对话中直接展示）
   - 文件模式：用户明确要求时生成 Word/Excel/HTML 统一文件
4. **内容更新**：references/extracted_data.json 需要定期同步Excel最新版本
